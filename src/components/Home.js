import React, { Component } from "react";
import { inject, observer } from "mobx-react";

class Home extends Component {
  render() {
    return <div className="HomeRoot">Home</div>;
  }
}

export default Home;
